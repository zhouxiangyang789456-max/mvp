# 2026-09-05 工作日志

## 阶段 4 计划已写好

**文件**：`D:/prounity/mvp/计划/HandMapBuilder阶段4旋转高度层级可见性方案.md`

### 三个用户反馈卡点 + 真因诊断

1. **地形方向只有一个**
   - 真因：`HandPlacedTile.RotationY` 字段早就存在，但 UI/放置逻辑都没读过
   - 数据模型零改动，只需补 UI + 写入

2. **没法调整地形高度**
   - 真因：`HandPlacedTile` 没有 `HeightOffset` 字段；`GridToWorld` 硬编码 `Y = z`
   - 方案：加字段 + 全局 `LayerHeightScale` 滑块

3. **添加层级之后没有反应（关键 bug）**
   - 真因：Z=1 网格画在 Y=1，但被 Z=0 prefab 完全挡住 + Scene 视图默认不抬头看
   - 不是"没反应"，是**视觉上看不到新层级**
   - 方案：添加层级时相机自动抬到新 Z 平面 + 闪烁高亮 + Z 轴引导线

### 6 大功能落地

- F1 旋转 4 方向按钮 + R/+90° 快捷键
- F2 旋转角自由输入框
- F3 单 tile HeightOffset 字段（数据扩展）
- F4 全局 LayerHeightScale（0.5~2.0）
- F5 添加层级自动 refocus 相机 + 闪烁 + Z 轴引导线
- F6 ghost preview 应用当前旋转
- F7 吸管带旋转回写

### 阶段 4 实施后多轮迭代（Bug 修复）

1. **"清空层级没反应"** → `ClearLevel` 只删 `_activeZ` 层；按钮文字改为 `清空当前层 (⌃⇧F) Z=N · X个`，新增"清空所有层级"按钮 + Dialog 显示删除数量
2. **"为什么点清空地图？"** → 用户想真正重置；直接用 python 脚本改写 asset YAML 把 Tiles 数组清空（先备份到 HandMap_New.before-reset.backup.asset）
3. **"添加层级视角变了但不出现第二层级"** → OnGUI 末尾的同步逻辑会把 `_maxZInData` 强制改回 0；新增 `_userReservedMaxZ` 字段实现"只增不减"；闪烁 box 位置改用 `_activeZ`
4. **"添加层级视角变了 / 鼠标无法移动"** → `FocusSceneViewOnLevel` 太激进：改 size / rotation / orthographic 全覆盖了用户视角；改为**只改 pivot 的 Y**，其他保留
5. **"按住鼠标右键无法操控视角"（本轮）** → 真凶不是按键过滤，是 `HandleUtility.AddDefaultControl(ctrlId)` 在 MouseMove/MouseDrag 阶段**强制接管所有未消费事件**，包括 SceneView orbit。**彻底删除这两行**，事件流回归：
   - 左键 MouseDown/Drag/Up：`e.Use()`（画/擦/选中）
   - 右键 MouseDown：`e.Use()`（吸管）
   - 其余 MouseMove/MouseDrag：**不消费**，SceneView 接管相机控制
   - 用户用 **Alt+左键拖动**（Unity 标准） orbit / 中键拖动 pan / 滚轮 zoom

### 阶段 5 已实施（尺寸确认 + 框选复制）

**F1 尺寸确认**：
- 加 `_pendingWidth / _pendingHeight / _gridSizeInited` 字段
- `DrawGridSizeField` 重写：pending 缓存 + 「应用尺寸 (↩)」按钮 +「恢复」按钮 + 1~200 范围限制 + 实时显示 "待应用：X × Y (+NxN)" HelpBox
- `ApplyGridSize()`：扫描裁剪 tile 数，弹警告二次确认，Undo 整批改名 "应用地图尺寸 AxB → CxD"

**F2 框选**：
- 新增 `_boxSelStart / _boxSelEnd / _selectedTileIndices: List<int>`
- `HandleSceneEvents` 重写：左键 MouseDown 记录 start，MouseDrag 检测距离 ≥2 格 → 切到 BoxSelecting 状态（不再画 tile），MouseUp 切到 BoxSelected
- `DrawBoxSelectionVisual()`：蓝色半透 `DrawSolidRectangleWithOutline` + 4 角白色 SphereHandleCap + 每个已选 tile 白色 wire cube

**F3-F4 复制入口与状态机**：
- `enum BuilderState { Idle, BoxSelecting, BoxSelected, ReadyToCopy }`，切换 `EnterReadyToCopyState / ExitCopyPasteState`
- `struct CopyGroupEntry { x, y, z, prefabPath, rotY, hOff }`（相对坐标 = 原坐标 - 原组最小 X/Y）
- `_clipboard: List<CopyGroupEntry>` + `_clipboardOrigin` + `_clipboardSize`

**F5-F6 UI**：
- `DrawBoxSelectionControls()` 加在 DrawDrawControls 后、DrawTransformControls 前：4 按钮 [复制 (⏎) / 粘贴 (Space) / 删除 (⌫) / 退出 (Esc)] + 状态 miniLabel（根据状态变色）+ 待粘贴 HelpBox
- 切换 asset 时清选区 + 复制组

**F7 Ghost Preview**：
- `DrawPasteGhostPreview`：每 tile 半透蓝色 wire cube (0.35, 0.78, 1, 0.7) + 整体亮黄 wire box (1, 0.95, 0.3, 0.9) + 📋 待粘贴 N 个标签
- 落点 = 当前 hover cell 当作"复制组原点"，shift 切换激活层 ghost 在新层显示

**F8 粘贴**：
- `PasteAtCurrentHover()`：所有 tile 按 F2-A 贴到 `_activeZ`（先切层后粘贴），越界自动裁剪 + 同坐标冲突跳过 + 单步 Undo (`Undo.SetCurrentGroupName("粘贴 N 个 tile @ Z=Y")`)
- `RefreshLayerPreviews(int z)`：只销毁+重建单 Z 预览（避免全图 Rebuild）
- `GuessCategoryFromPath` 把 HandMapCategory 转 HandTileCategory 用 `(HandTileCategory)(int)kv.Key`

**F9 快捷键**：
- Enter / ⏎ = 进入待粘贴态（仅在 BoxSelected）
- Space = 实贴（仅在 ReadyToCopy）
- Esc = 退出 ReadyToCopy 或清 BoxSelected
- Delete / Backspace = 删除选区（带 Undo，DestroyImmediate 所有 preview + RemoveAll Tiles）

### 阶段 5 关键参数
- 文件：`Assets/Editor/HandMapBuilder/HandMapBuilderWindow.cs`
- 方法数：41 → 52（+11）
- 文件行：1551 → 2249（+698）
- enum 转换：`HandMapCategory → HandTileCategory` = `(HandTileCategory)(int)`（两个 enum 名字不同但语义一致）

### 阶段 5 验证清单
- 拖拽 ≥2 格 = 框选；拖拽 ≤1 格 = 画（不误触发）
- Copy → Enter / Space / 单击 实贴
- 待粘贴态鼠标 hover → ghost 跟随；切层 (激活层按钮) → ghost 重画在新 Z
- Esc → 退出复制粘贴态，下次鼠标单击仍为画 tile
- 改窄地图弹警告显示 "会裁掉 N 个 tile"，可取消
- 50×50 地图整图框选 + Copy + 切层 + Space 实贴，全程 60fps

### 9 个实施步骤（共 ~2 小时）

预计总工作量 ~2 小时改造代码。

## 阶段 4 已实施 ✅

### 改动汇总

| 文件 | 改动 |
|------|------|
| `HandAuthoredMapData.cs` | `HandPlacedTile` 加 `HeightOffset` 字段（默认 0，不破坏已有 11 个 tile 数据）|
| `HandMapBuilderWindow.cs`（1432 行）| +300 行 |

### 新增功能（阶段 4）

| 功能 | 实现 |
|------|------|
| F1 旋转 4 方向 | `DrawTransformControls` 加 0/90/180/270 按钮，默认吸附 |
| F2 任意角度 | FloatField 输入框，打破吸附后 4 按钮全灭 |
| F3 高度微调 | `HeightOffset` [-2, +2] 滑块 + Shift+左键选中 → Inspector 区 |
| F4 层高缩放 | `LayerHeightScale` [0.5, 2.0]，所有已放 tile 同步重定位 |
| **F5 添加层级可见性** | **相机自动 pivot 到新 Z 中心 + 新层闪烁 2 秒 + Scene 4 角橙色 Z 轴引导线** |
| F6 ghost 旋转 | `EnsurePreviewFor` 读 `tile.RotationY` 应用到 transform |
| F7 吸管带全状态 | 同步回写 `RotationY` + `_rotationSnapped` + `HeightOffset` |

### 快捷键分配

- `R` = 旋转 +90°
- `⇧R` (Shift+R) = 旋转 -90°
- `H` = 高度偏移 +0.1
- `⇧H` (Shift+H) = 高度偏移 -0.1
- Shift+左键 = 选中该 tile 进 Inspector（不绘制）

### 关键 Bug 修复

"添加层级之后没有反应" 真因：Z=1 网格画在 Y=1 平面，被 Z=0 prefab 完全挡住 + Scene 视图相机不抬头看。修复 = 相机 refocus + 闪烁 + 引导线三重提示。

### 用户要做的事

1. Unity 重新编译（应该自动，重启 Unity 也行）
2. 关闭并重开工具窗口
3. 测试：旋转 / 高度 / 加层级（应自动出现）；验证 Shift+左键 Inspector
4. 下一阶段（5）：运行时集成 — `LevelMapGenerationProfile.HandMapOverride` + `ProceduralBattleMapProvider` 改造，让 BattleScene 真正使用 HandMapData

## Bug 修复 — "清空层级没有反应"

**用户报告**：点击"清空整个层级 (⌃⇧F)"按钮没反应，地形还在。

### 真因诊断

`ClearLevel()` 按 `t.Z == _activeZ` 删除——**只删当前激活层级**。

用户之前点过"⬇ 添加层级"按钮 → `_activeZ=1`，但所有 tile 都在 Z=0（共 140 个）。
点清空层级 → 删的是 Z=1（空的）→ `RemoveAll` 返回 0 → **看起来没反应**。

不是代码 bug，是**UX 没把"当前激活层级"显示在按钮上**。

### 修复（`HandMapBuilderWindow.cs`）

1. **新增** `CountTilesAtZ(int z)` 辅助方法 — 统计整个 Z 层 tile 数
2. **改进** `ClearLevel()` — Dialog 显示 tile 数量；删 0 个时弹"该层为空"提示
3. **改进** 按钮文字 → `清空当前层 (⌃⇧F)  Z=N · X个` 实时显示
4. **新增** `ClearAllLevels()` — 一键清空所有层级（红色按钮 + 二次确认）
5. **更新** `DrawHelp()` 文本匹配新按钮语义

文件改动 ~80 行（`HandMapBuilderWindow.cs`）。

## 第二个发现 — 磁盘 vs 内存数据不一致

**用户反馈**：点击改进后的按钮显示 "Z=0 · 0个"，但 Scene 视图仍显示大量地形。

### 根因
工具的 `_mapData`（内存里）**已被之前 ClearLevel 清空**（删 140 个 tile），但用户**没点保存**，所以磁盘 `HandMap_New.asset` 还保留 140 个 tile。下次 Unity 重启或重新打开工具时，磁盘数据又会被加载。

更糟的是：Scene 视图的预览实例（`_spawnedByCell`）**和内存 `_mapData.Tiles` 状态不一致**——预览还存在，但内存数据已空。

### 处理

直接修改磁盘 asset 文件——把 `Tiles:` 数组清空为 `Tiles: []`：

```python
# 找到 '  Tiles:' 行 + 后续 tile 条目（4 空格或 '  -' 开头），直到下一个顶层字段
new_lines = lines[:tiles_line_idx] + ['  Tiles: []'] + lines[next_top_idx:]
```

- 修改前：998 行 / 20789 字节 / 140 个 tile
- 修改后：18 行 / 462 字节 / 0 个 tile
- 备份：`D:/prounity/mvp/HandMap_New.before-reset.backup.asset`

### 用户接下来要做的

1. 回 Unity，Unity 会自动检测文件变化并重新导入 asset
2. **重新打开工具窗口**（Tools/Map Generation/Hand Map Builder）→ 让 `_mapData` 重新加载
3. Scene 视图的残留预览实例需要**手动清掉**：在 Hierarchy 里搜 `Tile_` 选中全部 → Delete，或直接新场景（"新场景"按钮）
4. 现在可以从干净状态开始重建地图

## 第三个 Bug 修复 — "添加层级视角变了但第二层不出现"

**用户报告**：点"⬇ 添加层级"按钮 → 视角转变（相机移到新 Z 平面）→ 但 UI 按钮只剩"第1层"，没出现"第2层"。

### 真因（双重 bug）

**Bug 1（致命）：OnGUI 末尾的同步逻辑强制覆盖 `_maxZInData`**

```csharp
// OnGUI() 末尾（line 110-120）
if (_mapData != null) {
    int newMax = 0;
    for (int i = 0; i < _mapData.Tiles.Count; i++) // ← 0 个 tile → newMax=0
        if (_mapData.Tiles[i].Z > newMax) newMax = _mapData.Tiles[i].Z;
    if (newMax != _maxZInData) {                    // 0 != 1 → 强行覆盖
        _maxZInData = newMax;                       // ← 把用户刚设的 1 改回 0
        if (_activeZ > _maxZInData) _activeZ = _maxZInData;  // _activeZ 也被夹回 0
    }
}
```

**Bug 2（次要）：闪烁代码 `Repaint()` 不能触发 SceneView 重绘**

```csharp
if (_levelAddTime > 0) {
    ...
    Handles.DrawWireCube(...);
    Repaint();  // ← EditorWindow.Repaint()，不是 SceneView.Repaint()，Scene 视图不会持续重绘
}
```

`Repaint()`（不指定对象）重绘的是当前 EditorWindow（即 HandMapBuilderWindow），不会触发 SceneView 重绘。所以闪烁 wire cube 实际只画了一帧。

### 修复（`HandMapBuilderWindow.cs`）

1. **新增** `int _userReservedMaxZ` 字段 — 记录用户主动保留的层级
2. **OnGUI 末尾同步逻辑** 改为 `newMax = Mathf.Max(newMax, _userReservedMaxZ)`，只增不减
3. **AddLevel / + 快捷键 / RemoveHighestZ / ClearLevel / ClearAllLevels / CreateNewAsset / 新场景** 全部同步维护 `_userReservedMaxZ`
4. **闪烁代码** 把 `Repaint()` 改成 `SceneView.lastActiveSceneView.Repaint()` —— Scene 持续重绘
5. **闪烁 box 位置** 从 `_maxZInData` 改成 `_activeZ`（更准确画在新加的那层）
6. **AddLevel** 加 `Debug.Log` 方便排查

### 教训

- 任何"OnGUI 末尾同步状态"逻辑，**只增不减**是更安全的策略——用户主动设置的状态不应被"数据扫描"覆盖
- SceneView 内的动画必须用 `SceneView.Repaint()` 才能持续刷新
- Repaint() 默认是当前 EditorWindow，不能跨窗口触发重绘
- **别在 `FocusSceneViewOnLevel` 里强制设置用户的相机 size/rotation/orthographic** —— 会破坏用户已配置的视角
- **鼠标事件里没过滤 Alt 修饰** → Alt+右键（zoom）、Alt+左键松开等 SceneView 相机操作会被吞掉
- 用户报"鼠标没法移动"通常是相机被强制改到一个很远的 size + 奇怪的 rotation，看起来地图很小、鼠标像没响应

## 阶段 5 修复（索引失效 + 粘贴越界）

### 用户反馈
用户 Console 异常刷屏：
- `ArgumentOutOfRangeException: Index was out of range` at `HandMapBuilderWindow.cs:1488` (TryGetSelectionRect)
- 调用栈：`OnSceneGui → DrawBoxSelectionVisual → TryGetSelectionRect`
- 日志时序：复制 170 → 粘贴（落点 110,36 超出 50×50 → 全部越界 → "放置 0 个"）→ 清空所有层级（20013 个）→ 后续每次 SceneView 重绘都抛

### 真因

- `_selectedTileIndices` 存的是 tile 索引，但 `ClearLevel / ClearAllLevels / RemoveHighestZ / ApplyGridSize` 把 tile 删掉后**索引就失效了**，下次 SceneView 重绘访问 `_mapData.Tiles[idx]` 抛 ArgumentOutOfRangeException
- 一旦异常抛一次，IMGUI 内部 Group/Clip 栈被破坏 → 后续每次绘制都报 "GUI Error: You are pushing more GUIClips than you are popping"
- 复制组超出地图边界时，paste 时把所有 170 个都"越界裁掉"→ "放置 0 个"，UX 不友好

### 修复（`HandMapBuilderWindow.cs`）

1. **统一方法 `InvalidateSelectionReferences()`** — 清空 `_selectedTileIndices` + `_clipboard` + 状态回 Idle
2. **`ClearLevel` / `ClearAllLevels` / `RemoveHighestZ` / `ApplyGridSize` 缩窄后** 全部调用 `InvalidateSelectionReferences`
3. **`TryGetSelectionRect` 防御性索引检查** — 倒序遍历同时剔除失效索引
4. **`DrawBoxSelectionVisual` 已选 tile 高亮循环** 同样防御
5. **`PasteAtCurrentHover` 完全越界拦截** — 复制组完全超出地图时不再生成"放置 0 个"的刷屏日志
6. **`DrawPasteGhostPreview` Ghost 标签显示 "X/Y ⚠越界 Z"** — 让用户清楚能看到越界几个、贴几个

### 教训

- **任何 `List<int>` 存储数据集合的索引都必须考虑：数据集合可能在你不知情时被外部变更**（用户点清空、缩窄地图、撤销）
- 防御方案：①每次访问前验证 idx 范围并清理；②重大变更后统一清理
- 一旦 IMGUI 异常，**GUI clip 错误会作为副作用持续刷屏**——主异常修了它就停
- 任何"自动剪裁"操作要给用户**可见提示**（不是 silent skip）

---

## 阶段 6 已规划（运行时集成：HandMap → BattleScene）

**计划文档**：`D:\prounity\mvp\计划\HandMapBuilder阶段6运行时集成方案.md`（~340 行,9 大节）

### 探索发现的关键事实

1. `LevelMapGenerationProfile.HandMapOverride` 字段**只在注释里被提到** — 类里没有声明
2. `ProceduralBattleMapProvider` 是 `static` 类,**纯程序化,从不读 HandAuthoredMapData**
3. `BattleGridController.ResolveMap()` 是**唯一需要打补丁的入口**
4. `HandPlacedTile.PrefabPath` 存 `Assets/Isometric Pack 3d/...` 这种 Editor-only 路径,运行时 `Resources.Load` 加载不到
5. `TerrainType` 有 10 种（Plain/Forest/Hill/Mountain/SnowMountain/Desert/ShallowWater/Ocean/Road/Bridge）
6. `CommanderSelectController._levelIndex + _mapProfile` 走 `BattleStartContext.MapProfile` 跨场景
7. 现成 `HandMapBattleMapProvider` / `IBattleMapProvider` **完全没有**

### 7 个决策点（用户已选 3 个，其余采用推荐）

| 决策 | 用户选择 | 落地 |
|------|---------|------|
| 1. Prefab 引用方式 | **A. 直接 `[SerializeField] GameObject Prefab`** | `HandPlacedTile` 加字段,一次性 Editor 工具回填所有现存 .asset |
| 2. 是否抽 `IBattleMapProvider` 接口 | A. 抽接口 | `IBattleMapProvider.BuildMap(...)`,Procedural/HandMap 都实现 |
| 3. 关卡配置粒度 | A. `LevelMapGenerationProfile.HandMapOverride` 加 1 字段 | 配在"按关卡选规则"的资产上 |
| 4. HandTileCategory → TerrainType 映射 | 12 类映射表（Building→Plain+SetBlocked,Plant/Effect→Plain 视觉等） | `TerrainCatalog.ResolveFromHandCategory` helper |
| 5. Z 层渲染 + **桥能走人** | **5B + Bridge Z-Aware 提升** | 所有 Z 按 Y 偏移渲染,Z>=1 Bridge 时把该格 TerrainType 提升为 Bridge,IsWalkable 不动 |
| 6. 切换优先级 | A. 扩 `BattleMapSource.HandAuthored` enum | `_mapSource=HandAuthored` 优先,缺 HandMapOverride → 报错回退 |
| 7. 回填方式 | A. Editor 工具 `Tools/HandMapBuilder/Refill Prefab References` | 一次性扫所有 HandMap,`AssetDatabase.LoadAssetAtPath` 填 Prefab |

### 阶段 6 总改动量

- 新增 ~280 行（5 文件）
- 改动 ~110 行（6 文件）
- 1 个新 enum 值（`BattleMapSource.HandAuthored`）
- 1 个一次性回填工具

### Bridge Z-Aware 提升实现（用户提的"桥能走人"的方案 1）

```csharp
static readonly HashSet<HandTileCategory> _zAwareWalkableCategories
    = new HashSet<HandTileCategory> { HandTileCategory.Bridge, HandTileCategory.Ramp };
// 任何 Z>=1 tile 是 Bridge/Ramp 时,把该 (x,y) 最终 TerrainType 提升为对应可走类型
```

零编辑器改动,零 IsWalkable 改动,零 pathfinder 改动。代价：Bridge 必须"语义上跨水/山",不能拿来当 Plain 用。

---

## 阶段 7 已规划（运行时视觉升级 2D sprite → 3D mesh）

**计划文档**：`D:\prounity\mvp\计划\HandMapBuilder阶段73D视觉升级方案.md`（~430 行,10 大节）

**用户原话**："其实我更想用3d资源，这样好一点"

### 现状核心矛盾

- **编辑器侧**：用户已经精心用 3D IsoPack 模型（树、栅栏、砖块）调好地图
- **运行时侧**：BattleScene 仍是 2D sprite 平铺,**所见非所得**

### 8 个决策点（3 个待用户确认,5 个采用推荐）

| 决策 | 推荐 | 说明 |
|------|------|------|
| 1. ground prefab 池 | **A. `TerrainPrefabCatalog` ScriptableObject** | 每种 TerrainType 配 1~N 个 prefab + 权重 |
| 2. Road 拼接 | **B. 保留拼接,换 3D Road prefab** | mask 还在但用 prefab 替换 |
| 3. 水/海洋 | A. IsoPack WaterTile + transparent shader | renderQueue=3001 避免 sort |
| 4. 相机 | **A. 保留 ortho 斜俯视** | IsoPack 为 ortho 设计,不动 BattleCameraController |
| 5. Z-fight | **A. 错位 + 复用 `NormalizeAndGround`** | 抽到 `TerrainGeometryHelper` 静态工具 |
| 6. 旧 `TerrainVisualCatalog` | **A. 保留作 fallback** | prefab-first, sprite 兜底,可逐步升级 |
| 7. UnitView/Decorations 联动 | A. 不动 | 已经是 3D, 只看 scale 是否协调 |
| 8. UI/Canvas | A. 不动 | 2D UI 与地形视觉无关 |

### 阶段 7 与阶段 6 的关系

- **正交独立**：阶段 6 数据层,阶段 7 视觉层
- **有冲突**：阶段 6 计划 `HandMapVisualRenderer` 实例化 HandMap tile prefab,阶段 7 也把 ground 改 prefab → **会重复**
- **协同方案**：阶段 7 在 `HandMapVisualRenderer` 加 `_managedByBattleGrid` 白名单 `{Base/Path/Water/Mountain}`,只渲染 Building/Decoration/Effect 等立体 tile

### 执行顺序建议

**先阶段 6 后阶段 7**（推荐）：每段独立可测,符合 "先解决能不能用,再解决好不好看"

### 阶段 7 总改动量

- 新增 ~250 行（4 文件）
- 改动 ~50 行（4 文件）
- 1 个一次性 Editor 工具 `TerrainPrefabCatalogSetup` 扫 IsoPack 自动配 prefab 池

### 关键文件清单（阶段7）

- 新增 `Assets\Scripts\Battle\Map\TerrainGeometryHelper.cs`（抽 NormalizeAndGround）
- 新增 `Assets\Scripts\Battle\Map\TerrainPrefabCatalog.cs` ScriptableObject
- 新增 `Assets\Resources\Battle\Terrain\Generated\TerrainPrefabCatalog.asset`
- 新增 `Assets\Editor\TerrainPrefabCatalogSetup.cs` [MenuItem]
- 改 `Assets\Scripts\Battle\Map\TerrainVisualCatalog.cs` 加 `HasPrefab(TerrainType)`
- 改 `Assets\Scripts\Battle\Map\BattleCellView.cs` prefab-first + sprite fallback
- 改 `Assets\Scripts\Battle\Map\BattleGridController.cs` 删 BaseGround sprite
- 改 `Assets\Scripts\Battle\Map\Decorations\TerrainDecorationSpawner.cs` 改调 helper

