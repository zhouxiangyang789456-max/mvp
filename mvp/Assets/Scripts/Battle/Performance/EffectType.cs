namespace Mvp.Battle
{
    /// <summary>Pooled effect kinds (性能文档：EffectPool).</summary>
    public enum EffectType
    {
        MuzzleFlash,
        HitSpark,
        BulletTracer,
        InvalidClickFlash
    }
}
