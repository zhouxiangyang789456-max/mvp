namespace Mvp.Shared
{
    /// <summary>Runtime state of a unit.</summary>
    public enum UnitState
    {
        Idle,
        Moving,
        Chasing,
        Attacking,
        Deploying,
        Capturing,
        Dead
    }
}
